CC = gcc
SRC = main.c school.c
OBJ = ${SRC:%.c=%.o}
NAME = school
RM = rm -f

all: ${OBJ}
	${CC} ${OBJ} -o ${NAME}

%.o: %.c
	${CC} -c -o $@ $<

re:
	oclean all

clean:
	${RM} *~ ${NAME} 

oclean: ${OBJ}
	${RM} ${OBJ}

fclean:
	oclean clean

